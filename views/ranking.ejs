<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ranking</title>
    <link href="/css/end.css" rel="stylesheet">
    <link href="/css/headerStyle.css" rel="stylesheet">
</head>
<body style="min-height:100vh; background: linear-gradient(120deg, #e0e7ff 0%, #fffbe6 100%); display:flex; align-items:center; justify-content:center;">
    <div class="rankingMode" style="min-height:90vh; min-width:350px; max-width:600px; margin:auto; display:flex; flex-direction:column; justify-content:center; box-shadow:0 4px 24px rgba(79,140,255,0.10); border-radius:1.5rem; background:#fff; padding:2.5rem 2rem 1.5rem 2rem;">
        <button class="ranking-back-btn" onclick="window.history.back()" title="뒤로가기" style="margin-bottom:1rem;">←</button>
        <div class="rainbow-title" style="text-align:center;">랭킹</div>
        <div style="text-align:center; margin-bottom:1.2rem;">
            <input type="text" id="ranking-search" placeholder="이름으로 검색" style="padding:0.5rem 1rem; border-radius:8px; border:1.5px solid #4f8cff; width:220px; font-size:1rem;">
        </div>
        <div class="ranking-lists-row" style="justify-content:center;">
            <div class="ranking-list-box" style="width:100%;">
                <h2 style="text-align:center;">사람</h2>
                <ul class="ranking-list" id="human-ranking-list">
                    <% for(let i=0; i<recode.length; i++) { %>
                        <li style="text-align:center;">
                            <span class="rank"><%= i + 1 %></span>
                            <span class="name"><%= recode[i].userName %></span>
                            <span class="time"><%= recode[i].time %>초</span>
                            <span class="count"><%= recode[i].cnt %>회</span>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
        <div class="footer" style="margin-top:2rem; text-align:center; color:#aaa;">© 유민이</div>
    </div>
    <script src="/javaScript/test.js"></script>
</body>
</html>