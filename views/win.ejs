<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>승리!</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/headerStyle.css" rel="stylesheet">
    <link href="/css/end.css" rel="stylesheet">
</head>
<body>
    <%- include("header.ejs") %>
    <div class="mainPage">
        <div></div>
        <div>
            <div class="card text-center py-5 px-4" style="max-width:420px;margin:2rem auto;">
                <div style="font-size:3.5rem; margin-bottom:1.2rem;">🏆</div>
                <h1 class="rainbow-title mb-4">승리하셨습니다!</h1>
                <h2 class="mb-2">⏱️ <span class="text-success"><%= time %></span>초</h2>
                <h2 class="mb-4">❓ <span class="text-primary"><%= cnt %></span>회</h2>
                <div class="input-group mb-4 w-75 mx-auto">
                    <span class="input-group-text">@</span>
                    <input type="text" class="form-control" placeholder="닉네임을 입력하세요" aria-label="Username">
                </div>
                <div class="btn-group w-100" role="group">
                    <button type="button" class="btn btn-outline-info recode">랭킹에 기록</button>
                    <button type="button" class="btn btn-outline-secondary" onclick="location.href='/'">메인화면</button>
                    <button type="button" class="btn btn-outline-success" onclick="location.href='/comment'">평가 남기기</button>
                </div>
            </div>
        </div>
        <div></div>
    </div>
    <script>
        window.winTime = "<%= time %>";
        window.winCnt = "<%= cnt %>";
    </script>
    <script src="/javaScript/test.js"></script>
</body>
</html>