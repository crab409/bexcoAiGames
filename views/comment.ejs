<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>평가 남기기</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/headerStyle.css" rel="stylesheet">
    <link href="/css/end.css" rel="stylesheet">
</head>
<body>
    <%- include("header.ejs") %>
    <div class="mainPage">
        <div></div>
        <div>
            <div class="card my-record mb-4" style="max-width:420px;margin:2rem auto;">
                <h2 class="mb-3">📝 평가 남기기</h2>
                <div class="input-group mb-3">
                    <span class="input-group-text">@</span>
                    <input type="text" class="form-control" placeholder="닉네임을 입력하세요" aria-label="Username">
                </div>
                <div class="input-group">
                    <span class="input-group-text">내용</span>
                    <textarea class="form-control" aria-label="With textarea" placeholder="의견을 남겨주세요"></textarea>
                </div>
                <button type="button" class="btn btn-outline-success mt-3 w-100">제출</button>
            </div>
            <div class="card my-record" style="max-width:420px;margin:2rem auto;">
                <h2 class="mb-3">💬 평가 목록</h2>
                <ul class="list-group">
                    <% for(let i=0; i<comments.length; i++) { %> 
                        <li class="list-box list-group-item">
                            <h4><%= comments[i].userName %></h4>
                            <p><%= comments[i].content %></p>  
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
        <div></div>
    </div>
    <script src="/javaScript/test.js"></script>
</body>
</html>